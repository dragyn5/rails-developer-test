<ul>
  <% @users.each do |user| %>
    <li><%= user.email %></li>
    <li><%= link_to 'Edit', edit_user_path(user) %></li>
    <td><%= link_to 'Destroy', user_path(user),
            method: :delete,
            data: { confirm: 'Are you sure?' } %></td>
    <% end %>
</ul>